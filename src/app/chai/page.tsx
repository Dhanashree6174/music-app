export default function ChaiPage() {
  return <h1 className="text-3xl text-center mt-8">Chai Page</h1>;
}

// the layout.tsx file of app directory is not overwritten, both the layout files are used instead
